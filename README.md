# Piece of Code

## Requirements
1. Makefile
1. uv

## Local Development
1. Setup environment variables. Uncomment and replace the environment variables values in `.env` if there is a need to override the default values.
```sh
$ cp -i .env.example .env
```

2.  Run api service
```sh
$ make dev
```
